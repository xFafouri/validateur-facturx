{
  "name": "@facturx/db",
  "version": "0.1.0",
  "private": true,
  "description": "Prisma schema and client for the Factur-X platform.",
  "scripts": {
    "_comment": "generate and validate inject a placeholder DATABASE_URL. Prisma resolves datasource env vars even when it is only parsing the schema, and neither generating the client nor checking schema validity should require a database to be configured - CI and Docker builds have none. A real DATABASE_URL in the environment always wins.",
    "generate": "DATABASE_URL=${DATABASE_URL:-postgresql://placeholder:placeholder@localhost:5432/placeholder} prisma generate",
    "validate": "DATABASE_URL=${DATABASE_URL:-postgresql://placeholder:placeholder@localhost:5432/placeholder} prisma validate",
    "typecheck": "pnpm run validate",
    "migrate:dev": "prisma migrate dev",
    "migrate:deploy": "prisma migrate deploy",
    "studio": "prisma studio",
    "test": "echo \"no db tests yet\" && exit 0"
  },
  "devDependencies": {
    "prisma": "^6.2.1"
  },
  "dependencies": {
    "@prisma/client": "^6.2.1"
  }
}
